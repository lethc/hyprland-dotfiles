(defpoll curr_clips :interval "5s" "~/.config/hypr/themes/one/eww/scripts/curr-clips")

(defwidget clipboard []
    (box
        :class "clipboard-container"
        (label
            :text curr_clips
            :limit-width 15
        )
    )
    )
