general{
    disable_loading_bar = true
    hide_cursor = true
}
background {
    monitor =
    # path = ~/.config/hypr/assets/Wallpapers_default/Sunset.png
    path = ~/Wallpapers/home.jpg
    #color = rgb(23, 23, 23, 1)

    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    # blur_passes = 3 # 0 disables blurring
    blur_passes = 2 # 0 disables blurring
    blur_size = 4
    noise = 0.0117
    contrast = 0.9000 # Vibrant!!!
    brightness = 0.7000
    vibrancy = 0.5100
    vibrancy_darkness = 0.0
    # blur_passes = 3 # 0 disables blurring
    # blur_size = 4
    # noise = 0.0117
    # contrast = 0.2000 # Vibrant!!!
    # brightness = 0.8000
    # vibrancy = 0.2100
    # vibrancy_darkness = 0.0
}

# Hours
label {
    monitor =
    text = cmd[update:1000] echo "<b><big> $(date +"%H") </big></b>"
    color = rgba(202, 201, 221)
    font_size = 112
    font_family = Noto Sans Regular  
    shadow_passes = 3
    shadow_size = 4

    position = -120, -100 # -120 = to the left (120 = right), -100 position from top to bottom
    halign = center
    valign = top
    # position = 0, 220
    # halign = center
    # valign = center
}

# Minutes
label {
    monitor =
    text = cmd[update:1000] echo "<b><big> $(date +"%M") </big></b>"
    color = rgba(202, 201, 221)
    font_size = 112
    font_family = Noto Sans Regular 
    shadow_passes = 3
    shadow_size = 4

    position = 120, -100
    halign = center
    valign = top
    # position = 0, 80
    # halign = center
    # valign = center
}

# Separator
label {
    monitor =
    text = cmd[update:1000] echo "<b><big> : </big></b>"
    color = rgba(202, 201, 221)
    font_size = 112
    font_family = Noto Sans Regular 
    shadow_passes = 3
    shadow_size = 4

    position = 0, -100
    halign = center
    valign = top
}

# Today
label {
    monitor =
    text = cmd[update:18000000] echo "<b><big> "$(date +'%A')" </big></b>"
    color = rgba(202, 201, 221)
    font_size = 22
    font_family = Noto Sans Regular 

    # position = 0, 360
    position = 0, 130
    halign = center
    valign = bottom
}

# Week
label {
    monitor =
    text = cmd[update:18000000] echo "<b> "$(date +'%d %b')" </b>"
    color = rgba(202, 201, 221)
    font_size = 18
    font_family = Noto Sans Regular 

    # position = 0, 330
    position = 0, 100
    halign = center
    valign = bottom
}

# Degrees
# label {
#     monitor =
#     text = cmd[update:18000000] echo "<b>Feels like<big> $(curl -s 'wttr.in?format=%t' | tr -d '+') </big></b>"
#     color = rgba(202, 201, 221)
#     font_size = 18
#     font_family = Noto Sans Regular 
#
#     position = 0, 40
#     halign = center
#     valign = bottom
# }

input-field {
    monitor =
    size = 250, 50
    outline_thickness = 3

    dots_size = 0.26 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.64 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    dots_rouding = -1

    rounding = 22
    outer_color = rgba(353535)
    inner_color = rgba(202, 201, 221)
    font_color = rgba(10, 10, 10)
    fade_on_empty = true
    placeholder_text = <i>Password...</i> # Text rendered in the input box when it's empty.

    # position = 0, 120
    position = 0, 250 # Position from bottom to top
    halign = center
    valign = bottom
}
